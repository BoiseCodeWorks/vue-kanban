const actions = {
  create: 'Create',
  update: 'Update',
  remove: 'Remove',
  find: 'Find',
  findAll: 'Find All'
}

const models = {
  board: 'Board',
  list: 'List'
}


export  {
  actions,
  models
}